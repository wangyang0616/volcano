---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: repackruns.repack.volcano.sh
spec:
  group: repack.volcano.sh
  names:
    kind: RepackRun
    listKind: RepackRunList
    plural: repackruns
    shortNames:
    - rpk
    - repackrun
    singular: repackrun
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: MODE
      type: string
    - jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .status.report.summary.nodesFreed
      name: FREED
      type: integer
    - jsonPath: .status.report.summary.verdict
      name: VERDICT
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RepackRun is a one-shot runtime-defragmentation job. It is cluster-scoped and
          user-immutable (CREATE/READ/DELETE only): spec is frozen after admission via a
          CEL transition rule — to change anything, delete and create a new Run.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              Spec is immutable once set (one-shot job; recompute by creating a new Run).
              P1 will carve out controller-synced guardrail fields from this rule (§4.4.1).
            properties:
              activeDeadlineSeconds:
                description: ActiveDeadlineSeconds bounds Running time; exceeded ->
                  Failed (like Job).
                format: int64
                minimum: 1
                type: integer
              disruptionPolicy:
                description: 'DisruptionPolicy (P1): how/whether running jobs may
                  be disturbed.'
                properties:
                  bundlePolicy:
                    description: BundlePolicy controls the unit of relocation for
                      a gang (§4.13).
                    enum:
                    - SurplusPodsOnly
                    - EntireJobPermitted
                    type: string
                  hardFloors:
                    description: HardFloors are optional hard guardrails (frozen anchors
                      / per-job caps).
                    properties:
                      freezePriorityAbove:
                        description: 'FreezePriorityAbove: gangs with priority >=
                          this never move.'
                        format: int32
                        type: integer
                      maxMovesPerJob:
                        description: MaxMovesPerJob caps relocations of any single
                          PodGroup.
                        format: int32
                        minimum: 0
                        type: integer
                    type: object
                  lambda:
                    description: Lambda is the benefit-vs-friction trade-off as a
                      decimal string (e.g. "0.5").
                    type: string
                  maxDisruptionScore:
                    description: MaxDisruptionScore is the disruption-cost red line
                      (§4.13.3).
                    format: int32
                    minimum: 0
                    type: integer
                  minRunDuration:
                    description: 'MinRunDuration: jobs running less than this are
                      not moved.'
                    type: string
                  respectPDB:
                    description: RespectPDB enables PodDisruptionBudget compatibility
                      (§4.13.4).
                    type: boolean
                  weights:
                    additionalProperties:
                      type: string
                    description: |-
                      Weights are per-disruption-term weights as decimal strings (keys must match
                      scoring plugins enabled in config, §4.16.5).
                    type: object
                type: object
              goals:
                description: |-
                  Goals is the per-resource fragmentation target. P0/P1: exactly one entry
                  (a run defragments a single accelerator resource); multi-resource is P2+.
                items:
                  description: RepackGoal is one resource's fragmentation target.
                  properties:
                    minFragRateImprovement:
                      description: |-
                        MinFragRateImprovement is the required absolute drop in this resource's
                        fragmentation rate, as a decimal string (e.g. "0.05"). 0/empty = any benefit.
                      type: string
                    resource:
                      description: Resource is the accelerator to defragment (e.g.
                        nvidia.com/gpu).
                      type: string
                  required:
                  - resource
                  type: object
                maxItems: 1
                type: array
              maxPerRun:
                description: MaxPerRun caps the blast radius of a single run.
                properties:
                  podGroups:
                    description: PodGroups caps distinct PodGroups moved (cross-resource
                      count).
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: Resources caps accelerator cards moved per resource.
                    type: object
                type: object
              mode:
                description: Mode selects DryRun (simulate + report) or Execute (act).
                enum:
                - DryRun
                - Execute
                type: string
              relief:
                description: 'Relief (P1): pending PodGroups this run aims to make
                  schedulable.'
                properties:
                  minRelieved:
                    description: 'MinRelieved: at least this many must become schedulable
                      to be worthwhile.'
                    format: int32
                    minimum: 1
                    type: integer
                  podGroupRefs:
                    description: PodGroupRefs are "namespace/name" of pending PodGroups
                      to relieve.
                    items:
                      type: string
                    maxItems: 256
                    type: array
                type: object
              scope:
                description: Scope bounds which PodGroups may move and which nodes
                  participate.
                properties:
                  nodes:
                    description: RepackSelectorTerm is an include/exclude pair (exclude
                      wins).
                    properties:
                      exclude:
                        description: RepackSelector matches by label selector and/or
                          explicit names (union).
                        properties:
                          names:
                            description: Names are "namespace/name" for PodGroups,
                              or node names for nodes.
                            items:
                              type: string
                            maxItems: 1024
                            type: array
                          selector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      include:
                        description: RepackSelector matches by label selector and/or
                          explicit names (union).
                        properties:
                          names:
                            description: Names are "namespace/name" for PodGroups,
                              or node names for nodes.
                            items:
                              type: string
                            maxItems: 1024
                            type: array
                          selector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                    type: object
                  podGroups:
                    description: RepackSelectorTerm is an include/exclude pair (exclude
                      wins).
                    properties:
                      exclude:
                        description: RepackSelector matches by label selector and/or
                          explicit names (union).
                        properties:
                          names:
                            description: Names are "namespace/name" for PodGroups,
                              or node names for nodes.
                            items:
                              type: string
                            maxItems: 1024
                            type: array
                          selector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                      include:
                        description: RepackSelector matches by label selector and/or
                          explicit names (union).
                        properties:
                          names:
                            description: Names are "namespace/name" for PodGroups,
                              or node names for nodes.
                            items:
                              type: string
                            maxItems: 1024
                            type: array
                          selector:
                            description: |-
                              A label selector is a label query over a set of resources. The result of matchLabels and
                              matchExpressions are ANDed. An empty label selector matches all objects. A null
                              label selector matches no objects.
                            properties:
                              matchExpressions:
                                description: matchExpressions is a list of label selector
                                  requirements. The requirements are ANDed.
                                items:
                                  description: |-
                                    A label selector requirement is a selector that contains values, a key, and an operator that
                                    relates the key and values.
                                  properties:
                                    key:
                                      description: key is the label key that the selector
                                        applies to.
                                      type: string
                                    operator:
                                      description: |-
                                        operator represents a key's relationship to a set of values.
                                        Valid operators are In, NotIn, Exists and DoesNotExist.
                                      type: string
                                    values:
                                      description: |-
                                        values is an array of string values. If the operator is In or NotIn,
                                        the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                        the values array must be empty. This array is replaced during a strategic
                                        merge patch.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  required:
                                  - key
                                  - operator
                                  type: object
                                type: array
                                x-kubernetes-list-type: atomic
                              matchLabels:
                                additionalProperties:
                                  type: string
                                description: |-
                                  matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                  map is equivalent to an element of matchExpressions, whose key field is "key", the
                                  operator is "In", and the values array contains only "value". The requirements are ANDed.
                                type: object
                            type: object
                            x-kubernetes-map-type: atomic
                        type: object
                    type: object
                type: object
              ttlSecondsAfterFinished:
                description: |-
                  TTLSecondsAfterFinished: auto-DELETE this Run that long after it finishes
                  (like Job). Unset = not auto-deleted (P0; Policy default is P1).
                format: int64
                minimum: 0
                type: integer
            required:
            - mode
            type: object
            x-kubernetes-validations:
            - message: RepackRun.spec is immutable; create a new RepackRun to change
                it
              rule: self == oldSelf
          status:
            description: |-
              RepackRunStatus reports lifecycle and business output. conditions are
              authoritative; phase is derived (§4.6).
            properties:
              completionTime:
                description: CompletionTime is when the run first reached a terminal
                  phase (TTL anchor).
                format: date-time
                type: string
              conditions:
                description: |-
                  Conditions are the authoritative facts (Job-style: Admitted/Queued/
                  Progressing/Complete/Failed/Cancelled), §4.6.1.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message is a one-line human verdict written at terminal
                  state.
                type: string
              mode:
                description: Mode mirrors spec.mode (observed) for list/printer convenience.
                enum:
                - DryRun
                - Execute
                type: string
              nominations:
                description: |-
                  Nominations are the durable landing-steering intents produced by Execute:
                  one entry per relocated pod. The repack-engine's nomination reconciler
                  watches the replacement (recreated) pods and patches their
                  pod.status.nominatedNodeName to Node, matching by PodName (deterministic-
                  name controllers, e.g. vcjob `<job>-<task>-<index>` / StatefulSet) or by
                  Gang+Role (fungible-name controllers). Each is consumed once and expires at
                  ExpireAt. See §4.7.1.2 of the design.
                items:
                  description: NominationRecord is one relocated pod's landing-steering
                    intent (§4.7.1.2).
                  properties:
                    expireAt:
                      description: |-
                        ExpireAt bounds how long the reconciler keeps re-asserting this nomination
                        before giving up (drift is then reported). Unset = engine default.
                      format: date-time
                      type: string
                    gang:
                      description: Gang is the PodGroup ref ("namespace/name") for
                        fungible-name matching.
                      type: string
                    namespace:
                      description: Namespace of the pod.
                      type: string
                    node:
                      description: Node is the target node to nominate the replacement
                        onto.
                      type: string
                    phase:
                      description: |-
                        Phase tracks consumption: Pending (not yet matched) / Bound (patched onto a
                        replacement) / Expired (TTL elapsed without a match).
                      enum:
                      - Pending
                      - Bound
                      - Expired
                      type: string
                    podName:
                      description: |-
                        PodName is the evicted victim's pod name; deterministic-name controllers
                        recreate the replacement with the SAME name (exact-match path).
                      type: string
                    role:
                      description: Role is the task role (volcano.sh/task-spec) within
                        the gang.
                      type: string
                  required:
                  - namespace
                  - node
                  type: object
                maxItems: 4096
                type: array
              observedGeneration:
                description: ObservedGeneration is the metadata.generation observed
                  after Admit.
                format: int64
                type: integer
              phase:
                description: Phase is a derived projection of conditions (§4.6.1).
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Cancelled
                type: string
              report:
                description: Report is the DryRun output (recommendation only).
                properties:
                  generatedAt:
                    format: date-time
                    type: string
                  recommendedNodes:
                    description: RecommendedNodes is the per-node detail (which nodes
                      would be freed).
                    items:
                      description: RecommendedNode is one node the plan would free
                        or draw from.
                      properties:
                        freed:
                          description: Freed indicates the node ends up fully empty
                            under the plan.
                          type: boolean
                        nodeName:
                          type: string
                      required:
                      - nodeName
                      type: object
                    maxItems: 2048
                    type: array
                  recommendedPodGroups:
                    description: RecommendedPodGroups is the per-gang detail (node-pair
                      aggregated, not per-pod).
                    items:
                      description: RecommendedPodGroup is one gang the DryRun suggests
                        moving.
                      properties:
                        disruptionScore:
                          format: int32
                          type: integer
                        moveKind:
                          description: MoveKind is how a gang is relocated (mirrors
                            bundlePolicy).
                          enum:
                          - SurplusPods
                          - WholeGroup
                          type: string
                        podGroupRef:
                          description: PodGroupRef is "namespace/name".
                          type: string
                        podsToMove:
                          description: PodsToMove is gang-safe (does not breach MinAvailable
                            unless WholeGroup).
                          format: int32
                          type: integer
                        podsTotal:
                          format: int32
                          type: integer
                        reason:
                          type: string
                        recommended:
                          type: boolean
                      required:
                      - podGroupRef
                      type: object
                    maxItems: 2048
                    type: array
                  relief:
                    description: Relief reports which pending PodGroups would be unblocked
                      (P1).
                    items:
                      description: RelievedPodGroup reports a pending gang that would
                        become schedulable (P1).
                      properties:
                        podGroupRef:
                          type: string
                        relieved:
                          type: boolean
                      required:
                      - podGroupRef
                      type: object
                    maxItems: 256
                    type: array
                  summary:
                    description: Summary is the flat, at-a-glance layer (UI lists/alerts
                      read this).
                    properties:
                      fragRateAfter:
                        type: string
                      fragRateBefore:
                        description: Aggregate (cross-resource) WeightedFragRate values,
                          as decimal strings.
                        type: string
                      fragRateDelta:
                        type: string
                      nodesFreed:
                        format: int32
                        type: integer
                      pendingRelieved:
                        format: int32
                        type: integer
                      perResource:
                        additionalProperties:
                          description: PerResourceSummary is one accelerator's fragmentation/movement
                            figures.
                          properties:
                            fragRateAfter:
                              type: string
                            fragRateBefore:
                              type: string
                            fragRateDelta:
                              type: string
                            moved:
                              description: Moved is this resource's relocated card
                                count.
                              format: int64
                              type: integer
                            nodesFreed:
                              format: int32
                              type: integer
                          type: object
                        description: PerResource holds per-accelerator fragmentation/movement
                          detail.
                        type: object
                      podGroupsToMove:
                        format: int32
                        type: integer
                      resolvedScope:
                        description: ResolvedScope reports the effective scope after
                          selector resolution.
                        properties:
                          nodeCount:
                            format: int32
                            type: integer
                          podGroupCount:
                            format: int32
                            type: integer
                        type: object
                      verdict:
                        description: RepackVerdict says whether repacking is worthwhile.
                        enum:
                        - Recommended
                        - NoBenefit
                        type: string
                    type: object
                type: object
              result:
                description: Result is the Execute output (what actually moved).
                properties:
                  executedAt:
                    format: date-time
                    type: string
                  freedNodes:
                    description: FreedNodes are the nodes actually emptied.
                    items:
                      type: string
                    maxItems: 2048
                    type: array
                  repackedPodGroups:
                    description: RepackedPodGroups records the gangs actually relocated.
                    items:
                      description: RecommendedPodGroup is one gang the DryRun suggests
                        moving.
                      properties:
                        disruptionScore:
                          format: int32
                          type: integer
                        moveKind:
                          description: MoveKind is how a gang is relocated (mirrors
                            bundlePolicy).
                          enum:
                          - SurplusPods
                          - WholeGroup
                          type: string
                        podGroupRef:
                          description: PodGroupRef is "namespace/name".
                          type: string
                        podsToMove:
                          description: PodsToMove is gang-safe (does not breach MinAvailable
                            unless WholeGroup).
                          format: int32
                          type: integer
                        podsTotal:
                          format: int32
                          type: integer
                        reason:
                          type: string
                        recommended:
                          type: boolean
                      required:
                      - podGroupRef
                      type: object
                    maxItems: 2048
                    type: array
                  summary:
                    description: RepackSummary is the flat second layer of the report.
                    properties:
                      fragRateAfter:
                        type: string
                      fragRateBefore:
                        description: Aggregate (cross-resource) WeightedFragRate values,
                          as decimal strings.
                        type: string
                      fragRateDelta:
                        type: string
                      nodesFreed:
                        format: int32
                        type: integer
                      pendingRelieved:
                        format: int32
                        type: integer
                      perResource:
                        additionalProperties:
                          description: PerResourceSummary is one accelerator's fragmentation/movement
                            figures.
                          properties:
                            fragRateAfter:
                              type: string
                            fragRateBefore:
                              type: string
                            fragRateDelta:
                              type: string
                            moved:
                              description: Moved is this resource's relocated card
                                count.
                              format: int64
                              type: integer
                            nodesFreed:
                              format: int32
                              type: integer
                          type: object
                        description: PerResource holds per-accelerator fragmentation/movement
                          detail.
                        type: object
                      podGroupsToMove:
                        format: int32
                        type: integer
                      resolvedScope:
                        description: ResolvedScope reports the effective scope after
                          selector resolution.
                        properties:
                          nodeCount:
                            format: int32
                            type: integer
                          podGroupCount:
                            format: int32
                            type: integer
                        type: object
                      verdict:
                        description: RepackVerdict says whether repacking is worthwhile.
                        enum:
                        - Recommended
                        - NoBenefit
                        type: string
                    type: object
                type: object
              startTime:
                description: StartTime is when the run first entered Running.
                format: date-time
                type: string
              triggerReason:
                description: TriggerReason is the observed creation cause (Manual
                  / OnPending… ).
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
