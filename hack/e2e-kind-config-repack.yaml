# Minimal kind config for repack E2E (no DRA/MutatingAdmissionPolicy feature gates).
# Use this when running on kind versions that default to Kubernetes < 1.34.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker
