---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: repackpolicies.repack.volcano.sh
spec:
  group: repack.volcano.sh
  names:
    kind: RepackPolicy
    listKind: RepackPolicyList
    plural: repackpolicies
    shortNames:
    - rpp
    - repackpolicy
    singular: repackpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.suspend
      name: SUSPEND
      type: boolean
    - jsonPath: .status.lastTriggerTime
      name: LAST-TRIGGER
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RepackPolicy generates RepackRuns on a trigger, embedding a RepackRun
          template (CronJob -> Job). Cluster-scoped.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RepackPolicySpec declares when to trigger and what RepackRun
              to generate.
            properties:
              failedRunsHistoryLimit:
                format: int32
                minimum: 0
                type: integer
              runTemplate:
                description: |-
                  RunTemplate is the RepackRun to create (reuses RepackRunSpec).
                  Whether the derived Run is DryRun or Execute is decided solely by
                  runTemplate.spec.mode (set mode: DryRun to only auto-produce reports);
                  Execute serialization/cooldown is enforced by the engine (K=1 + cooldown).
                properties:
                  metadata:
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        type: object
                      finalizers:
                        items:
                          type: string
                        type: array
                      labels:
                        additionalProperties:
                          type: string
                        type: object
                      name:
                        type: string
                      namespace:
                        type: string
                    type: object
                  spec:
                    description: |-
                      RepackRunSpec is the user-facing, self-contained spec of one repack job.
                      P0 is self-contained (hand-written in full); relief and disruptionPolicy are
                      P1 — fields are reserved here so the schema is stable, but the engine
                      honors engine defaults in P0.

                      Admission is enforced entirely at the apiserver via CEL/markers (no controller
                      admission step): mode is an enum, goals is capped at one entry, the spec is
                      immutable, and the rule below requires Execute to name a non-empty scope on at
                      least one axis (a blanket whole-cluster Execute is rejected).
                    properties:
                      disruptionPolicy:
                        description: 'DisruptionPolicy (P1): how/whether running jobs
                          may be disturbed.'
                        properties:
                          bundlePolicy:
                            description: BundlePolicy controls the unit of relocation
                              for a gang.
                            enum:
                            - SurplusPodsOnly
                            - EntireJobPermitted
                            type: string
                          hardFloors:
                            description: HardFloors are optional hard guardrails (frozen
                              anchors / per-job caps).
                            properties:
                              freezePriorityAbove:
                                description: 'FreezePriorityAbove: gangs with priority
                                  >= this never move.'
                                format: int32
                                type: integer
                              maxMovesPerJob:
                                description: MaxMovesPerJob caps relocations of any
                                  single PodGroup.
                                format: int32
                                minimum: 0
                                type: integer
                            type: object
                          lambda:
                            description: Lambda is the benefit-vs-friction trade-off
                              as an integer weight (default 1).
                            format: int32
                            minimum: 0
                            type: integer
                          maxDisruptionScore:
                            description: MaxDisruptionScore is the disruption-cost
                              red line.
                            format: int32
                            minimum: 0
                            type: integer
                          minRunDuration:
                            description: 'MinRunDuration: jobs running less than this
                              are not moved.'
                            type: string
                          respectPDB:
                            description: RespectPDB enables PodDisruptionBudget compatibility.
                            type: boolean
                          weights:
                            additionalProperties:
                              format: int32
                              type: integer
                            description: |-
                              Weights are per-disruption-term integer weights (relative; keys must match
                              scoring plugins enabled in config).
                            type: object
                        type: object
                      goals:
                        description: |-
                          Goals is the per-resource fragmentation target. P0/P1: exactly one entry
                          (a run defragments a single accelerator resource); multi-resource is P2+.
                        items:
                          description: RepackGoal is one resource's fragmentation
                            target.
                          properties:
                            minFragImprovementPercent:
                              description: |-
                                MinFragImprovementPercent is the required minimum drop in this resource's
                                fragmentation, in percentage points (0-100). 0 = any benefit counts.
                              format: int32
                              maximum: 100
                              minimum: 0
                              type: integer
                            resource:
                              description: Resource is the accelerator to defragment
                                (e.g. nvidia.com/gpu).
                              type: string
                          required:
                          - resource
                          type: object
                        maxItems: 1
                        type: array
                      maxPerRun:
                        description: MaxPerRun caps the blast radius of a single run.
                        properties:
                          podGroups:
                            description: PodGroups caps distinct PodGroups moved (cross-resource
                              count).
                            format: int32
                            minimum: 0
                            type: integer
                          resources:
                            additionalProperties:
                              anyOf:
                              - type: integer
                              - type: string
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            description: Resources caps accelerator cards moved per
                              resource.
                            type: object
                        type: object
                      mode:
                        description: Mode selects DryRun (simulate + report) or Execute
                          (act).
                        enum:
                        - DryRun
                        - Execute
                        type: string
                      relief:
                        description: 'Relief (P1): pending PodGroups this run aims
                          to make schedulable.'
                        properties:
                          minRelieved:
                            description: 'MinRelieved: at least this many must become
                              schedulable to be worthwhile.'
                            format: int32
                            minimum: 1
                            type: integer
                          podGroupRefs:
                            description: PodGroupRefs are "namespace/name" of pending
                              PodGroups to relieve.
                            items:
                              type: string
                            maxItems: 256
                            type: array
                        type: object
                      scope:
                        description: Scope bounds which PodGroups may move and which
                          nodes participate.
                        properties:
                          nodes:
                            description: RepackSelectorTerm is an include/exclude
                              pair (exclude wins).
                            properties:
                              exclude:
                                description: RepackSelector matches by label selector
                                  and/or explicit names (union).
                                properties:
                                  names:
                                    description: Names are "namespace/name" for PodGroups,
                                      or node names for nodes.
                                    items:
                                      type: string
                                    maxItems: 1024
                                    type: array
                                  selector:
                                    description: |-
                                      A label selector is a label query over a set of resources. The result of matchLabels and
                                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                                      label selector matches no objects.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                              include:
                                description: RepackSelector matches by label selector
                                  and/or explicit names (union).
                                properties:
                                  names:
                                    description: Names are "namespace/name" for PodGroups,
                                      or node names for nodes.
                                    items:
                                      type: string
                                    maxItems: 1024
                                    type: array
                                  selector:
                                    description: |-
                                      A label selector is a label query over a set of resources. The result of matchLabels and
                                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                                      label selector matches no objects.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            type: object
                          podGroups:
                            description: RepackSelectorTerm is an include/exclude
                              pair (exclude wins).
                            properties:
                              exclude:
                                description: RepackSelector matches by label selector
                                  and/or explicit names (union).
                                properties:
                                  names:
                                    description: Names are "namespace/name" for PodGroups,
                                      or node names for nodes.
                                    items:
                                      type: string
                                    maxItems: 1024
                                    type: array
                                  selector:
                                    description: |-
                                      A label selector is a label query over a set of resources. The result of matchLabels and
                                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                                      label selector matches no objects.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                              include:
                                description: RepackSelector matches by label selector
                                  and/or explicit names (union).
                                properties:
                                  names:
                                    description: Names are "namespace/name" for PodGroups,
                                      or node names for nodes.
                                    items:
                                      type: string
                                    maxItems: 1024
                                    type: array
                                  selector:
                                    description: |-
                                      A label selector is a label query over a set of resources. The result of matchLabels and
                                      matchExpressions are ANDed. An empty label selector matches all objects. A null
                                      label selector matches no objects.
                                    properties:
                                      matchExpressions:
                                        description: matchExpressions is a list of
                                          label selector requirements. The requirements
                                          are ANDed.
                                        items:
                                          description: |-
                                            A label selector requirement is a selector that contains values, a key, and an operator that
                                            relates the key and values.
                                          properties:
                                            key:
                                              description: key is the label key that
                                                the selector applies to.
                                              type: string
                                            operator:
                                              description: |-
                                                operator represents a key's relationship to a set of values.
                                                Valid operators are In, NotIn, Exists and DoesNotExist.
                                              type: string
                                            values:
                                              description: |-
                                                values is an array of string values. If the operator is In or NotIn,
                                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                                the values array must be empty. This array is replaced during a strategic
                                                merge patch.
                                              items:
                                                type: string
                                              type: array
                                              x-kubernetes-list-type: atomic
                                          required:
                                          - key
                                          - operator
                                          type: object
                                        type: array
                                        x-kubernetes-list-type: atomic
                                      matchLabels:
                                        additionalProperties:
                                          type: string
                                        description: |-
                                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                                        type: object
                                    type: object
                                    x-kubernetes-map-type: atomic
                                type: object
                            type: object
                        type: object
                      ttlSecondsAfterFinished:
                        description: |-
                          TTLSecondsAfterFinished: auto-DELETE this Run that long after it finishes
                          (like Job). Unset = not auto-deleted (P0; Policy default is P1).
                        format: int64
                        minimum: 0
                        type: integer
                    required:
                    - mode
                    type: object
                    x-kubernetes-validations:
                    - message: Execute requires spec.scope with a non-empty include
                        on podGroups or nodes
                      rule: self.mode != 'Execute' || (has(self.scope) && ((has(self.scope.podGroups)
                        && has(self.scope.podGroups.include) && (has(self.scope.podGroups.include.selector)
                        || (has(self.scope.podGroups.include.names) && size(self.scope.podGroups.include.names)
                        > 0))) || (has(self.scope.nodes) && has(self.scope.nodes.include)
                        && (has(self.scope.nodes.include.selector) || (has(self.scope.nodes.include.names)
                        && size(self.scope.nodes.include.names) > 0)))))
                required:
                - spec
                type: object
              successfulRunsHistoryLimit:
                description: |-
                  SuccessfulRunsHistoryLimit / FailedRunsHistoryLimit cap how many finished
                  derived Runs are retained (flat, mirroring CronJob; default 3 each).
                format: int32
                minimum: 0
                type: integer
              suspend:
                description: Suspend pauses triggering (does not affect already-generated
                  Runs).
                type: boolean
              trigger:
                description: Trigger is when to emit a RepackRun (three sources; any
                  match fires).
                properties:
                  cronSchedule:
                    description: 'CronSchedule is a cron expression: fires on schedule.'
                    type: string
                  onFragmentation:
                    description: |-
                      OnFragmentation fires when the fragmentation rate exceeds a threshold
                      (reactive).
                    properties:
                      fragAbovePercent:
                        description: |-
                          FragAbovePercent fires when the fragmentation rate is above this percentage
                          (0-100). FragRate is this design's fragmentation metric; see the design
                          proposal's fragmentation-metric section.
                        format: int32
                        maximum: 100
                        minimum: 0
                        type: integer
                      minPendingPodGroups:
                        description: |-
                          MinPendingPodGroups is an optional additional gate: also require at least
                          this many PodGroups pending before firing.
                        format: int32
                        minimum: 1
                        type: integer
                    required:
                    - fragAbovePercent
                    type: object
                  onPendingBlocked:
                    description: |-
                      OnPendingBlocked fires when gangs cannot schedule due to fragmentation
                      (reactive). "Due to fragmentation" means a repack plan exists that would
                      make them schedulable (repack can actually help) — not that the cluster is
                      simply full — to avoid useless triggers.
                    properties:
                      minBlockedDuration:
                        description: |-
                          MinBlockedDuration: and blocked continuously for at least this long
                          (debounce).
                        type: string
                      minPendingPodGroups:
                        description: |-
                          MinPendingPodGroups: at least this many PodGroups (gangs) blocked by
                          fragmentation before firing. Default 1.
                        format: int32
                        minimum: 1
                        type: integer
                    type: object
                type: object
                x-kubernetes-validations:
                - message: trigger must set at least one of cronSchedule/onPendingBlocked/onFragmentation
                  rule: has(self.cronSchedule) || has(self.onPendingBlocked) || has(self.onFragmentation)
            required:
            - runTemplate
            - trigger
            type: object
          status:
            description: |-
              RepackPolicyStatus reports triggering bookkeeping (mirrors CronJob's active[]
              / lastScheduleTime shape; LastTriggerTime is used instead of lastScheduleTime
              because triggers are multi-source, not schedule-only).
            properties:
              active:
                description: Active are the derived RepackRuns currently in progress.
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
                x-kubernetes-list-type: atomic
              conditions:
                description: Conditions is the authoritative status detail.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastEvaluationTime:
                description: LastEvaluationTime is when the controller last evaluated
                  the trigger.
                format: date-time
                type: string
              lastTriggerTime:
                description: LastTriggerTime is when a RepackRun was last generated.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
